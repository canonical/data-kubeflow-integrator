juju:
  agent-version: 3.6.9 # TODO: Unpin once Spark works on new juju release
  model-defaults:
    logging-config: <root>=INFO; unit=DEBUG
providers:
  lxd:
    enable: true
    bootstrap: true
host:
  packages:
    - pipx
  snaps:
    charmcraft:
      channel: latest/stable
    jhack:
      channel: latest/edge
      connections:
        - jhack:dot-local-share-juju snapd
